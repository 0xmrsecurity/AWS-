## EBS (Elastic Block Storage) Enumeration stuff !!!
>[!CAUTION]
> Elastic Block Storage, service provided by aws. It simple mean that we can store virtual hard Drives.


>[!NOTE]
> It allows you to create and attach virtual hard drives (called EBS volumes) to your EC2 instances, just like connecting a physical hard drive to a computer.
